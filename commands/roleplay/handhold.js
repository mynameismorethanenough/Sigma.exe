const { buildRoleplay } = require('./_builder');
module.exports = buildRoleplay({ name: 'handhold', aliases: ['holdhand'], apiType: 'handhold', emoji: '🤝', color: 0xffd700, action: 'holds hands with' });
